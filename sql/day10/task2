create table student15(
  sname varchar(20),
  smark int
);

INSERT  INTO student15(sname,smark)values
     ('Priya',95),
     ('Harish',85),
     ('sumi',70),
     ('sneha',80),
('hari',75);

select *
from Student15
where smark=(select max(smark)
              from Student15
              );
